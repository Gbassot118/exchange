# =============================================================================
# Coolify Environment Variables - Exchange (Documentation Collaborative)
# =============================================================================
# Copy these variables into Coolify's environment configuration.
# Generate secrets using: openssl rand -hex 32 (for APP_SECRET)
#                        openssl rand -base64 32 (for JWT secrets)
# =============================================================================

# === DOMAINE ===
# Ton domaine public (pour Mercure, les URLs absolues, etc.)
PUBLIC_DOMAIN=exchange.example.com

# === APPLICATION ===
# Generer avec: openssl rand -hex 32
APP_SECRET=change_me_with_openssl_rand_hex_32

# === BASE DE DONNEES ===
POSTGRES_DB=exchange
POSTGRES_USER=app
POSTGRES_PASSWORD=change_me_secure_password

# === MERCURE ===
# Generer avec: openssl rand -base64 32
MERCURE_JWT_SECRET=change_me_mercure_jwt_secret

# === APIS IA ===
OPENAI_API_KEY=sk-proj-your-openai-key
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key
