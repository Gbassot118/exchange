<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Documentation Collaborative{% endblock %}</title>

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- HTMX -->
    <script src="https://unpkg.com/htmx.org@2.0.0"></script>
    <script src="https://unpkg.com/htmx-ext-sse@2.2.0/sse.js"></script>

    <!-- Custom styles -->
    <style>
        [data-loading] { display: none; }
        .htmx-request [data-loading] { display: inline-block; }
        .htmx-request [data-loaded] { display: none; }

        .annotation-highlight {
            background-color: rgba(251, 191, 36, 0.3);
            border-bottom: 2px solid #f59e0b;
        }

        /* Code blocks */
        .document-content pre {
            background-color: #1f2937;
            border-radius: 0.5rem;
            padding: 1rem;
            overflow-x: auto;
            white-space: pre-wrap;
            word-wrap: break-word;
            margin: 1rem 0;
        }

        .document-content pre code {
            color: #e5e7eb;
            font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
            font-size: 0.875rem;
            background: none !important;
            padding: 0 !important;
            border-radius: 0 !important;
        }

        /* Inline code */
        .document-content code:not(pre code) {
            background-color: #f3f4f6;
            padding: 0.125rem 0.375rem;
            border-radius: 0.25rem;
            font-size: 0.875rem;
            font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
            color: #db2777;
        }

        /* Tables */
        .document-content table {
            border-collapse: collapse;
            width: 100%;
            margin: 1rem 0;
        }

        .document-content th,
        .document-content td {
            border: 1px solid #e5e7eb;
            padding: 0.75rem 1rem;
            text-align: left;
        }

        .document-content th {
            background-color: #f9fafb;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.75rem;
            color: #6b7280;
        }

        .document-content tr:hover {
            background-color: #f9fafb;
        }

        /* Blockquotes */
        .document-content blockquote {
            border-left: 4px solid #3b82f6;
            background-color: #eff6ff;
            padding: 1rem;
            margin: 1rem 0;
            font-style: italic;
        }

        .document-content blockquote p {
            margin: 0;
        }

        /* Task lists */
        .document-content input[type="checkbox"] {
            width: 1rem;
            height: 1rem;
            margin-right: 0.5rem;
            vertical-align: middle;
        }

        /* Horizontal rules */
        .document-content hr {
            border: none;
            border-top: 1px solid #e5e7eb;
            margin: 2rem 0;
        }

        /* Annotation content (compact markdown) */
        .annotation-content p {
            margin: 0.25rem 0;
        }

        .annotation-content p:first-child {
            margin-top: 0;
        }

        .annotation-content p:last-child {
            margin-bottom: 0;
        }

        .annotation-content strong {
            font-weight: 600;
        }

        .annotation-content em {
            font-style: italic;
        }

        .annotation-content code {
            background-color: #f3f4f6;
            padding: 0.125rem 0.25rem;
            border-radius: 0.25rem;
            font-size: 0.75rem;
            font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
        }

        .annotation-content pre {
            background-color: #1f2937;
            border-radius: 0.375rem;
            padding: 0.5rem;
            overflow-x: auto;
            margin: 0.5rem 0;
            font-size: 0.75rem;
        }

        .annotation-content pre code {
            color: #e5e7eb;
            background: none;
            padding: 0;
        }

        .annotation-content blockquote {
            border-left: 2px solid #3b82f6;
            background-color: #eff6ff;
            padding: 0.5rem;
            margin: 0.5rem 0;
            font-style: italic;
            font-size: 0.875rem;
        }

        .annotation-content blockquote p {
            margin: 0;
        }

        .annotation-content ul, .annotation-content ol {
            margin: 0.25rem 0;
            padding-left: 1rem;
        }

        .annotation-content li {
            margin: 0.125rem 0;
        }

        .annotation-content table {
            border-collapse: collapse;
            width: 100%;
            margin: 0.5rem 0;
            font-size: 0.75rem;
        }

        .annotation-content th, .annotation-content td {
            border: 1px solid #e5e7eb;
            padding: 0.25rem 0.5rem;
            text-align: left;
        }

        .annotation-content th {
            background-color: #f9fafb;
            font-weight: 600;
        }

        .annotation-content h1, .annotation-content h2, .annotation-content h3, .annotation-content h4 {
            font-weight: 600;
            margin: 0.5rem 0 0.25rem 0;
        }

        .annotation-content h1 { font-size: 1rem; }
        .annotation-content h2 { font-size: 0.9375rem; }
        .annotation-content h3 { font-size: 0.875rem; }
        .annotation-content h4 { font-size: 0.8125rem; }

        .annotation-content a {
            color: #2563eb;
            text-decoration: underline;
        }

        .annotation-content hr {
            border: none;
            border-top: 1px solid #e5e7eb;
            margin: 0.5rem 0;
        }
    </style>

    {% block stylesheets %}{% endblock %}
</head>
<body class="bg-gray-50 min-h-screen">
    {# Flash messages #}
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="fixed top-4 left-1/2 -translate-x-1/2 z-50 px-4 py-3 rounded-lg shadow-lg
                {% if label == 'error' %}bg-red-100 text-red-800 border border-red-200
                {% elseif label == 'success' %}bg-green-100 text-green-800 border border-green-200
                {% else %}bg-blue-100 text-blue-800 border border-blue-200{% endif %}"
                 onclick="this.remove()">
                <div class="flex items-center gap-2">
                    {% if label == 'error' %}
                        <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
                        </svg>
                    {% elseif label == 'success' %}
                        <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                        </svg>
                    {% endif %}
                    <span>{{ message }}</span>
                </div>
            </div>
        {% endfor %}
    {% endfor %}

    {% block body %}{% endblock %}

    {% block javascripts %}
    <script>
        // Auto-refresh presence heartbeat
        function startHeartbeat(sessionId, participantId) {
            setInterval(() => {
                const currentDocumentId = document.body.dataset.currentDocumentId || null;
                fetch(`/api/sessions/${sessionId}/heartbeat`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ participant_id: participantId, current_document_id: currentDocumentId })
                });
            }, 10000);
        }

    </script>
    {% endblock %}
</body>
</html>
