<div id="decisions-list" class="space-y-4">
    {% if decisions|default([])|length > 0 %}
        <div class="flex items-center gap-2 mb-4">
            <svg class="w-5 h-5 text-amber-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/>
            </svg>
            <h3 class="font-semibold text-gray-900">Points de décision</h3>
            <span class="px-2 py-0.5 text-xs bg-amber-100 text-amber-700 rounded-full">
                {{ decisions|length }}
            </span>
        </div>

        {% for decision in decisions %}
            {% include 'decision/_card.html.twig' with {'decision': decision, 'participant': participant} %}
        {% endfor %}
    {% endif %}
</div>
